[project]
name = "dealer-api"
version = "0.1.0"
description = "Dealer API Lambda for AgentCore Gateway integration"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.42.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
