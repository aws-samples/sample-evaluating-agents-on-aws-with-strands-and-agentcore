# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: MIT-0
"""Runnable SDK quickstart: a mock agent evaluated through all four layers.

A package rather than loose scripts so ``run_demo.py`` can import
``quickstart.mock_task_fn`` by name, which is also what the SDK's own
integration test executes.
"""
